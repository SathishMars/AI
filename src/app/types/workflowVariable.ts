

export interface WorkflowVariable {
    label: string;                                              // Variable label
    name: string;                                               // Variable name (to be used in the workflow)
}
